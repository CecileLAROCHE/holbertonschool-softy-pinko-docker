upstream api_servers {
    server back-end:5252;
}

server {
    listen 80;

    location / {
        proxy_pass http://front-end;
    }

    location /api {
        proxy_pass http://api_servers;
    }
}
